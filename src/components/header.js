import React from "react";
import styled from "styled-components";

const StyledNav = styled.nav`
  border: 1px solid white;
  border-radius: 4px;
`;

const Header = () => {
  return <StyledNav>Header</StyledNav>;
};

export default Header;
